{
  "name": "we-is-us",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "bun run validate && astro build",
    "preview": "astro preview",
    "validate": "bun run scripts/validate-schema.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "ajv": "^8.17.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "astro": "^5.15.9"
  }
}
